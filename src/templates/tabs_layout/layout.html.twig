{% extends 'layout.html.twig' %}

{% block mainFilters %}
  <turbo-frame id="tabs-nav" {{ stimulus_controller('tabs-nav') }} class="col-12 col-lg-5 col-xl-7">
    <div class="row">
      <!-- Navigation -->
      <div class="mt-3 mt-lg-2 flex-nowrap overflow-x-auto col-12 col-lg-6 col-xl-6">
        <nav class="nav flex-nowrap overflow-x-auto">
          {% block tabsNavigationLinks %} {% endblock %}
        </nav>
      </div>
      <!-- here is where filters can be rendered through the turbo stream rtrieved content -->
      <div id="top-right-filters" class="d-flex mt-3 mt-lg-2  col-12 col-lg-6 col-xl-6 justify-content-end"></div>

    </div>
  </turbo-frame>
{% endblock %}